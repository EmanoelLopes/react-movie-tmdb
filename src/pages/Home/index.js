import React, { Fragment } from 'react';
import { HomeContainer } from 'containers';

const Home = () => {
  return (
    <Fragment>
      <HomeContainer />
    </Fragment>
  );
};

export default Home;
